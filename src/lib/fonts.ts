import { Lato, Lobster } from 'next/font/google';

export const lato = Lato({
  weight: ['100', '400', '900'],
  subsets: ['latin'],
});

export const lobster = Lobster({
  weight: '400',
  subsets: ['latin'],
});
