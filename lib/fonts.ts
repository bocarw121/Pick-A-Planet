import { Lato, Lobster, Roboto } from 'next/font/google';

export const lato = Lato({
  weight: ['100', '400', '900'],
  subsets: ['latin'],
});

export const roboto = Roboto({
  weight: ['100', '400', '900'],
  subsets: ['latin'],
});

export const lobster = Lobster({
  weight: '400',
  subsets: ['latin'],
});
